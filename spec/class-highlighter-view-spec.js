'use babel';

import ClassHighlighterView from '../lib/class-highlighter-view';

describe('ClassHighlighterView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
